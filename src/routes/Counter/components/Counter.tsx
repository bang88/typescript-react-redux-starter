import * as React from 'react';

class Counter extends React.Component<any, any> {
    render() {
        return (
            <div>
                Counter
            </div>
        );
    }
}

export default Counter;
